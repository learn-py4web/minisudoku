"use strict";

let app = {};

app.data = {    
    data: function() {
        return {
        };
    },
    computed: {
    },
    methods: {
    }
};

app.vue = Vue.createApp(app.data).mount("#app");
